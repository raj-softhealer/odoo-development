<odoo>
  <data>

    <!-- form view -->
    <record id="view_book_return_form" model="ir.ui.view">
      <field name="name">book.return.form</field>
      <field name="model">book.return</field>
      <field name="arch" type="xml">
        <form string="Book Return">
          <sheet>

            <group>

              <field name="borrower_id" />
              <field name="book_id" widget="many2many_tags" />
              <field name="author_id" widget="many2many_tags" />
              <field name="borrow_date" />
              <field name="return_validity" />
              <field name="returned_on" />
              <field name="amount_payable" />


            </group>
          </sheet>
          <footer>
            <button string="Pay &amp; Return Book" type="object" name="action_return_book_wizard" class="oe_highlight"/>
            <button string="Cancel" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>


  </data>
</odoo>